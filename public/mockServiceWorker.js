// Mock Service Worker placeholder
// This file exists to prevent 404 errors from browser extensions or cached service worker registrations
// that are looking for Mock Service Worker (MSW) files.
// Since this application doesn't use MSW, this is an empty placeholder.

// No-op service worker registration
if (typeof self !== 'undefined' && 'serviceWorker' in navigator) {
  // Intentionally empty - no service worker functionality
}

